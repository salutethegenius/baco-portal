# =====================================
# BACO Membership Platform - Environment Variables
# =====================================
# Copy this file to .env and fill in your values

# =====================================
# DATABASE (Required)
# =====================================
# For local development with PostgreSQL:
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/baco_dev

# Individual PostgreSQL connection details (used by some tools)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=baco_dev

# =====================================
# SESSION (Required)
# =====================================
# Generate a secure random string (32+ characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_secure_session_secret_here_at_least_32_characters

# =====================================
# STRIPE (Optional - for payment processing)
# =====================================
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# For frontend (Vite requires VITE_ prefix)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# =====================================
# GOOGLE CLOUD STORAGE (Optional - for document uploads)
# =====================================
# Create a service account at https://console.cloud.google.com/
GOOGLE_CLOUD_PROJECT_ID=your_project_id
GOOGLE_CLOUD_BUCKET_NAME=your_bucket_name
GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account-key.json

# Alternative: Object Storage (Replit-specific, ignore for local)
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=
# AWS_S3_BUCKET=

# =====================================
# APPLICATION SETTINGS
# =====================================
NODE_ENV=development
PORT=5000

# =====================================
# PRODUCTION DEPLOYMENT (Vercel + Supabase)
# =====================================
# When deploying to Vercel, set these in the Vercel dashboard:
# - DATABASE_URL: Your Supabase PostgreSQL connection string
# - SESSION_SECRET: A secure production secret
# - STRIPE keys: Production Stripe keys
# - NODE_ENV=production
